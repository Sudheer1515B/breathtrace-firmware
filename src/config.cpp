#include "config.h"

// Panel order must match the physical left-to-right order of the cassette
// windows and therefore the MUX_TEST / MUX_CONTROL channel maps.
const char* const PANEL_NAME[PANEL_COUNT] = {"THC", "OPI", "MET", "AMP"};
